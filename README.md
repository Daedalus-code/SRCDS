# SRCDS
Source Dedicated Server shell script tool

Installed and built on Ubuntu  

install:  
```sudo apt-get install git -y```  
```cd && git clone https://github.com/Daedalus-code/SRCDS.git```  
```bash SRCDS/include/install```  
